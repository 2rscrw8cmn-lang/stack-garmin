# Deprecated — Runner Icon Integration

This document described the older OFFSET treatment where a one-color runner mark functioned as a small steps icon.

That direction is superseded by the current STACK Hero Time design.

Use:

- `TRAINER_BOI_INTEGRATION.md` for the full-color central Trainer Boi treatment
- `WATCH_FACE.md` for the current canonical watch-face direction

Do not implement the previous 30 px one-color runner slot unless a later metric icon specifically needs it.
