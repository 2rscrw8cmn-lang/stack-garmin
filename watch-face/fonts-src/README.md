# Local font source

Place the properly licensed source font here as:

`Skomelr Quantum.otf`

The OTF is intentionally gitignored because the demo supplied during design exploration is personal-use only.

Generate Garmin bitmap font resources with:

`python ../tools/gen_fonts.py`
